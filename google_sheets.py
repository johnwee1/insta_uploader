# Retrieve message from google sheets or wherever it may get from


def get_message() -> tuple[str, str]:
    """
    getMessage retrieves the given text from the google sheet
    :return: [name, message].
    """
    # potentially link to some kind of chatgpt api ?

    message = "a string to return"
    name = "a username string"
    return name, message
